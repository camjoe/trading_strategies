"""Shared repository and project path helpers.

Import from the submodules directly (``common.paths.repo_paths``,
``common.paths.project_paths``, …) rather than re-exporting here — an aggregate
surface only drifts out of sync with the modules it mirrors.
"""
