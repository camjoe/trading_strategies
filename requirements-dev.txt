-r requirements-base.txt

# Test dependencies used by local development and CI.
pytest==9.0.3
pytest-cov==6.0.0
pytest-mock==3.14.0
pytest-xdist>=3.8.0
hypothesis==6.131.26
httpx2>=2.9.1,<3.0.0

# Dev tooling
playwright>=1.50.0

# Schema migration tooling (ops-only by design — the application runtime never
# imports Alembic; ensure_db() checks alembic_version with plain SQL against a
# checked-in expected-head constant). See docs/adr/015-numbered-alembic-migrations.md.
alembic==1.18.5
sqlalchemy==2.0.51

# Quality tools invoked by scripts/run_checks and the CI quality gates.
# Pinned so local runs and .github/workflows/quality-gates.yml use the same versions.
ruff==0.15.21
mypy==2.3.0

# Editable install of this workspace so the apps/ and src/ packages
# (infrastructure, paper_trading_web, trends) import by their top-level names.
# Replaces the old pytest.ini `pythonpath` and scripts/__init__.py path hacks.
-e .
