"""Scripts package.

The ``src/`` and ``apps/`` packages (``trading``, ``common``, ``infrastructure``,
``paper_trading_web``, ``trends``) are made importable by the project's editable
install (``pip install -e .`` — see pyproject.toml), so no sys.path bootstrap is
needed here.
"""
